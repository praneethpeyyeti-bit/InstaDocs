{
  "name": "@instadocs/core",
  "version": "0.1.0",
  "description": "Platform-agnostic pipeline: repo -> detect -> parse -> analyze -> generate -> export.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "instadocs": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "docxtemplater": "^3.50.0",
    "exceljs": "^4.4.0",
    "fast-xml-parser": "^4.5.0",
    "pizzip": "^3.1.7",
    "simple-git": "^3.27.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
